/**
 * GitHub account management blade.
 * Shows user info, scopes, rate limits, linked repos, and sign out.
 * Full implementation in Task 2.
 */

export function GitHubAccountBlade() {
  return (
    <div className="p-4 text-ctp-text">
      <p>GitHub Account (loading...)</p>
    </div>
  );
}
