---
phase: 24-code-review-guidance-documentation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - docs/.vitepress/config.mts
  - docs/.vitepress/theme/index.ts
  - docs/index.md
  - docs/getting-started.md
  - docs/features/index.md
  - docs/features/gitflow.md
  - docs/features/staging.md
  - docs/features/branches.md
  - docs/reference/keyboard-shortcuts.md
  - docs/reference/settings.md
  - .github/workflows/docs.yml
  - .gitignore
autonomous: true

must_haves:
  truths:
    - "Documentation website builds successfully with npm run docs:build"
    - "Documentation uses Catppuccin Mocha theme matching the app aesthetic"
    - "Getting-started guide explains how to install and open the first repo"
    - "Feature overview covers Gitflow, staging, and branch management"
    - "Keyboard shortcuts reference lists all shortcuts from useKeyboardShortcuts.ts"
    - "GitHub Actions workflow deploys docs to GitHub Pages on push to main"
  artifacts:
    - path: "docs/.vitepress/config.mts"
      provides: "VitePress configuration with Catppuccin theme, nav, sidebar"
      contains: "base: '/FlowForge/'"
    - path: "docs/.vitepress/theme/index.ts"
      provides: "Catppuccin Mocha theme import"
      contains: "catppuccin/vitepress"
    - path: "docs/index.md"
      provides: "Landing page with hero section"
      contains: "hero"
    - path: "docs/getting-started.md"
      provides: "Installation and first-steps guide"
      contains: "Getting Started"
    - path: "docs/reference/keyboard-shortcuts.md"
      provides: "Full keyboard shortcuts reference table"
      contains: "Cmd/Ctrl"
    - path: ".github/workflows/docs.yml"
      provides: "GitHub Actions workflow for Pages deployment"
      contains: "deploy-pages"
  key_links:
    - from: "docs/.vitepress/config.mts"
      to: "docs/**/*.md"
      via: "sidebar and nav configuration pointing to content pages"
      pattern: "link:.*getting-started"
    - from: ".github/workflows/docs.yml"
      to: "docs/.vitepress/dist"
      via: "upload-pages-artifact action"
      pattern: "docs/.vitepress/dist"
    - from: "package.json"
      to: "docs/.vitepress/config.mts"
      via: "docs:dev, docs:build, docs:preview scripts"
      pattern: "docs:build"
---

<objective>
Scaffold a VitePress documentation website with Catppuccin theme and deploy it to GitHub Pages.

Purpose: New users can discover FlowForge through a published documentation website with a getting-started guide, feature overview, and keyboard shortcuts reference.

Output: Complete docs/ directory with VitePress config, 8 Markdown pages, Catppuccin Mocha theme, and GitHub Actions deployment workflow.
</objective>

<execution_context>
@/Users/phmatray/.claude/get-shit-done/workflows/execute-plan.md
@/Users/phmatray/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-code-review-guidance-documentation/24-RESEARCH.md

@src/hooks/useKeyboardShortcuts.ts (authoritative source for keyboard shortcuts)
@package.json (add docs scripts and devDependencies)
@.github/workflows/release.yml (reference for existing CI pattern)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install VitePress, scaffold config and core pages</name>
  <files>
    package.json
    docs/.vitepress/config.mts
    docs/.vitepress/theme/index.ts
    docs/index.md
    docs/getting-started.md
    .gitignore
  </files>
  <action>
    **Install dependencies:**

    ```bash
    cd /Users/phmatray/Repositories/github-phm/FlowForge
    npm install -D vitepress @catppuccin/vitepress
    ```

    **Add scripts to `package.json`:**

    Add these to the `scripts` section:
    - `"docs:dev": "vitepress dev docs"`
    - `"docs:build": "vitepress build docs"`
    - `"docs:preview": "vitepress preview docs"`

    **Update `.gitignore`:**

    Add these entries if not present:
    ```
    # VitePress
    docs/.vitepress/dist
    docs/.vitepress/cache
    ```

    **Create `docs/.vitepress/config.mts`:**

    ```typescript
    import { defineConfig } from 'vitepress'

    export default defineConfig({
      title: 'FlowForge',
      description: 'A modern, opinionated Git client for Gitflow workflows',
      base: '/FlowForge/',

      head: [
        ['link', { rel: 'icon', type: 'image/svg+xml', href: '/FlowForge/favicon.svg' }],
      ],

      themeConfig: {
        nav: [
          { text: 'Guide', link: '/getting-started' },
          { text: 'Features', link: '/features/' },
          { text: 'Reference', link: '/reference/keyboard-shortcuts' },
        ],
        sidebar: {
          '/features/': [
            {
              text: 'Features',
              items: [
                { text: 'Overview', link: '/features/' },
                { text: 'Gitflow', link: '/features/gitflow' },
                { text: 'Staging & Commits', link: '/features/staging' },
                { text: 'Branches', link: '/features/branches' },
              ],
            },
          ],
          '/reference/': [
            {
              text: 'Reference',
              items: [
                { text: 'Keyboard Shortcuts', link: '/reference/keyboard-shortcuts' },
                { text: 'Settings', link: '/reference/settings' },
              ],
            },
          ],
        },
        socialLinks: [
          { icon: 'github', link: 'https://github.com/Atypical-Consulting/FlowForge' },
        ],
        footer: {
          message: 'Built with Tauri, React, and Rust',
          copyright: 'Copyright 2026 Atypical Consulting',
        },
      },

      markdown: {
        theme: {
          light: 'catppuccin-latte',
          dark: 'catppuccin-mocha',
        },
      },
    })
    ```

    **Create `docs/.vitepress/theme/index.ts`:**

    ```typescript
    import DefaultTheme from "vitepress/theme";
    import "@catppuccin/vitepress/theme/mocha/mauve.css";

    export default DefaultTheme;
    ```

    **Create `docs/index.md`:**

    Landing page with VitePress hero frontmatter:
    - hero name: FlowForge
    - hero tagline: "A modern, opinionated Git client for Gitflow workflows"
    - hero text: Brief description — built on Tauri (Rust + React), enforces Gitflow conventions, conventional commits, and worktree management through its architecture
    - hero actions: "Get Started" link to /getting-started, "View on GitHub" link to the repo
    - Features section (3 features): Gitflow-First Design, Conventional Commits, Blade Navigation

    **Create `docs/getting-started.md`:**

    - Title: "Getting Started"
    - Prerequisites: macOS, Windows, or Linux
    - Installation section: Download from GitHub Releases (link to releases page), or build from source (clone + npm install + npm run tauri build)
    - "Open Your First Repository" section: Open FlowForge → Cmd/Ctrl+O or click Open → select a Git repository folder → explore the staging view
    - "Initialize Gitflow" section: If repo doesn't have Gitflow, FlowForge prompts to initialize with branch naming conventions
    - "Next Steps" section: links to Features and Keyboard Shortcuts
    - Keep concise: 300-500 words total
  </action>
  <verify>
    - `npm run docs:build` completes without errors
    - `docs/.vitepress/dist/index.html` exists
    - `docs/.vitepress/dist/getting-started.html` exists
    - Catppuccin theme CSS is included in the built output
  </verify>
  <done>
    VitePress documentation site scaffolded with Catppuccin Mocha theme, landing page with hero section, and getting-started guide. Build succeeds and produces static output.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create feature pages, reference pages, and GitHub Actions workflow</name>
  <files>
    docs/features/index.md
    docs/features/gitflow.md
    docs/features/staging.md
    docs/features/branches.md
    docs/reference/keyboard-shortcuts.md
    docs/reference/settings.md
    .github/workflows/docs.yml
  </files>
  <action>
    **Create `docs/features/index.md`:**

    Feature overview page listing FlowForge's main capabilities:
    - Gitflow Workflow Management — enforced through a state machine
    - Two-Column Staging — side-by-side changed files and diff preview
    - Conventional Commits — rule-based type/scope suggestions, real-time validation
    - Branch Management — pinning, recent branches, bulk cleanup
    - Blade Navigation — stack-based panels for commit details, diffs, settings, file browsing
    - Topology Graph — SVG+DOM hybrid visualization of branch history
    - Worktree Management — create, delete, and monitor multiple worktrees
    - Each feature gets 2-3 sentences and a link to its dedicated page (where available)

    **Create `docs/features/gitflow.md`:**

    - Title: "Gitflow Workflow"
    - How FlowForge enforces Gitflow: state machine in Rust, valid operations only
    - Branch types: main, develop, feature/*, release/*, hotfix/*
    - Starting flows: "Start Feature/Release/Hotfix" buttons in the Gitflow panel
    - Finishing flows: merge dialog with review checklist, auto-cleanup
    - Initialize Gitflow: prompt on non-Gitflow repos
    - 300-400 words

    **Create `docs/features/staging.md`:**

    - Title: "Staging & Commits"
    - Two-column layout: file list on left, inline diff preview on right
    - Stage/unstage individual files or folders
    - Conventional commit composer: type dropdown, scope autocomplete, message validation
    - Amend commit support (Cmd/Ctrl+Shift+M)
    - Full-screen diff blade with next/prev file navigation
    - 300-400 words

    **Create `docs/features/branches.md`:**

    - Title: "Branch Management"
    - Scope selector: Local, Remote, Last Used views
    - Pin/favorite branches for quick access
    - Recent branches tracked automatically
    - Bulk delete merged branches with Gitflow protection
    - Feature branch tags in purple across topology and branch list
    - 250-350 words

    **Create `docs/reference/keyboard-shortcuts.md`:**

    Extract ALL shortcuts from `src/hooks/useKeyboardShortcuts.ts` into a reference table. The authoritative list:

    **General:**
    | Shortcut | Action |
    |----------|--------|
    | `Cmd/Ctrl + O` | Open repository |
    | `Cmd/Ctrl + ,` | Open settings |
    | `Cmd/Ctrl + Shift + P` | Command palette |
    | `Escape` | Close current blade / navigate back |
    | `Backspace` | Navigate back |

    **Git Operations:**
    | Shortcut | Action |
    |----------|--------|
    | `Cmd/Ctrl + Shift + A` | Stage all files |
    | `Cmd/Ctrl + Shift + U` | Push to remote |
    | `Cmd/Ctrl + Shift + L` | Pull from remote |
    | `Cmd/Ctrl + Shift + F` | Fetch from remote |
    | `Cmd/Ctrl + Shift + M` | Toggle amend commit |

    **Topology:**
    | Shortcut | Action |
    |----------|--------|
    | `Enter` | Open details for selected commit |

    Add a note: "On macOS, `Cmd` is used. On Windows/Linux, `Ctrl` is used."

    **Create `docs/reference/settings.md`:**

    - Title: "Settings Reference"
    - General: Default tab (Changes/History/Topology)
    - Git: Default remote, auto-fetch interval
    - Integrations: External editor, terminal
    - Review: Checklist items per flow type (feature, release, hotfix)
    - Appearance: Theme toggle (Mocha/Latte)
    - Note: Settings are persisted locally via Tauri store
    - 200-300 words

    **Create `.github/workflows/docs.yml`:**

    GitHub Actions workflow for deploying VitePress to GitHub Pages:
    - Trigger: push to `main` when `docs/**` or `.github/workflows/docs.yml` changes, plus `workflow_dispatch`
    - Permissions: `contents: read`, `pages: write`, `id-token: write`
    - Concurrency group: `pages` with `cancel-in-progress: false`
    - Build job:
      - `actions/checkout@v4` with `fetch-depth: 0`
      - `actions/setup-node@v4` with `node-version: 20` and `cache: npm`
      - `npm ci`
      - `npm run docs:build`
      - `actions/upload-pages-artifact@v3` with `path: docs/.vitepress/dist`
    - Deploy job:
      - environment: `github-pages`
      - needs: build
      - `actions/deploy-pages@v4`

    Follow the exact pattern from the VitePress official docs / research document.
  </action>
  <verify>
    - `npm run docs:build` completes without errors (all pages resolve)
    - All 6 content files exist under `docs/`
    - `.github/workflows/docs.yml` exists with correct YAML syntax (`yamllint` or manual check)
    - Sidebar navigation links resolve to existing pages (no broken links)
    - Keyboard shortcuts table includes all 11 shortcuts from `useKeyboardShortcuts.ts`
  </verify>
  <done>
    Documentation website has feature pages (Gitflow, Staging, Branches), keyboard shortcuts reference with all 11 shortcuts, settings reference, and GitHub Actions workflow for automated deployment to GitHub Pages.
  </done>
</task>

</tasks>

<verification>
1. Run `npm run docs:build` — completes without errors
2. Run `npm run docs:preview` — site loads in browser
3. Verify landing page shows FlowForge hero with "Get Started" and "View on GitHub" buttons
4. Navigate to Getting Started — installation and first-repo instructions present
5. Navigate to Features > Overview — all features listed with links
6. Navigate to Features > Gitflow — Gitflow workflow guide present
7. Navigate to Reference > Keyboard Shortcuts — all 11 shortcuts listed in tables
8. Navigate to Reference > Settings — all settings categories documented
9. Verify Catppuccin Mocha dark theme is active (dark purple/mauve accent colors)
10. Verify `.github/workflows/docs.yml` is valid YAML with correct GitHub Actions syntax
</verification>

<success_criteria>
- VitePress documentation builds and serves locally without errors
- 8 Markdown content pages cover getting-started, feature overviews, and references
- Catppuccin Mocha theme is active with mauve accent
- GitHub Actions workflow is configured for automatic Pages deployment
- Keyboard shortcuts reference includes all 11 shortcuts from the app
- package.json has docs:dev, docs:build, docs:preview scripts
</success_criteria>

<output>
After completion, create `.planning/phases/24-code-review-guidance-documentation/24-02-SUMMARY.md`
</output>
