---
phase: 18-command-palette-discoverability
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/hooks/useKeyboardShortcuts.ts
  - src/components/sync/SyncButtons.tsx
autonomous: true

must_haves:
  truths:
    - "Push shortcut is mod+shift+u (not mod+shift+p) in both the hook binding and the SyncButtons tooltip"
    - "mod+shift+p is now free and available for the command palette"
    - "All other shortcuts remain unchanged"
  artifacts:
    - path: "src/hooks/useKeyboardShortcuts.ts"
      provides: "Push shortcut rebound to mod+shift+u"
      contains: "mod+shift+u"
    - path: "src/components/sync/SyncButtons.tsx"
      provides: "Push tooltip updated to mod+shift+U"
      contains: "mod+shift+U"
  key_links:
    - from: "src/hooks/useKeyboardShortcuts.ts"
      to: "src/components/sync/SyncButtons.tsx"
      via: "Both reference the Push shortcut string"
      pattern: "mod\\+shift\\+u"
---

<objective>
Reassign the Push keyboard shortcut from mod+shift+p to mod+shift+u to free up mod+shift+p for the command palette.

Purpose: mod+shift+p is the industry-standard shortcut for command palettes (VS Code, Sublime Text, JetBrains). It is currently bound to Push. Push must move to mod+shift+u ("Upload" mnemonic) before the palette shortcut can be registered.

Output: Push shortcut updated in two files. mod+shift+p is now unbound and available.
</objective>

<execution_context>
@/Users/phmatray/.claude/get-shit-done/workflows/execute-plan.md
@/Users/phmatray/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/hooks/useKeyboardShortcuts.ts
@src/components/sync/SyncButtons.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reassign Push shortcut from mod+shift+p to mod+shift+u</name>
  <files>src/hooks/useKeyboardShortcuts.ts, src/components/sync/SyncButtons.tsx</files>
  <action>
Make exactly THREE changes:

1. **src/hooks/useKeyboardShortcuts.ts** — JSDoc comment block (around line 18):
   Change `Cmd/Ctrl+Shift+P: Push` to `Cmd/Ctrl+Shift+U: Push (Upload)`

2. **src/hooks/useKeyboardShortcuts.ts** — useHotkeys call for Push (around line 128-129):
   Change `"mod+shift+p"` to `"mod+shift+u"` in the first argument of the useHotkeys call

3. **src/components/sync/SyncButtons.tsx** — ShortcutTooltip for Push button (around line 129):
   Change `shortcut="mod+shift+P"` to `shortcut="mod+shift+U"`

Do NOT change any other shortcuts. Do NOT change the push logic or any other code. This is a surgical, 3-line change.

Verify no other files reference "mod+shift+p" in a push context (grep for it to be safe).
  </action>
  <verify>
1. `grep -r "mod+shift+p" src/ --include="*.ts" --include="*.tsx"` — should return ZERO results (the shortcut should no longer appear anywhere)
2. `grep -r "mod+shift+u" src/ --include="*.ts" --include="*.tsx"` — should return exactly 1 result in useKeyboardShortcuts.ts
3. `grep -r "mod+shift+U" src/ --include="*.ts" --include="*.tsx"` — should return exactly 1 result in SyncButtons.tsx
4. `npx tsc --noEmit` passes (ignore pre-existing TS2440)
  </verify>
  <done>
Push shortcut is now mod+shift+u everywhere. mod+shift+p is free for the command palette. No other shortcuts affected.
  </done>
</task>

</tasks>

<verification>
1. Push shortcut binding in useKeyboardShortcuts.ts uses "mod+shift+u"
2. Push tooltip in SyncButtons.tsx displays "mod+shift+U"
3. JSDoc comment references "Cmd/Ctrl+Shift+U: Push"
4. No remaining references to "mod+shift+p" in source files
5. `npx tsc --noEmit` passes
</verification>

<success_criteria>
- mod+shift+p is completely unbound in the application
- Push works via mod+shift+u
- Push tooltip shows the new shortcut
</success_criteria>

<output>
After completion, create `.planning/phases/18-command-palette-discoverability/18-02-SUMMARY.md`
</output>
